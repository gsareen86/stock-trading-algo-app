# Backend API base URL. The browser reaches all data through this API — it never queries
# Supabase directly, and no database or Supabase key belongs in this file.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
