# Core
pandas>=2.0.0
numpy>=1.24.0
python-dateutil>=2.8.2
pytz>=2023.3
pyarrow>=11.0.0          # required for parquet cache (also a streamlit transitive dep)

# Market data (free)
yfinance>=0.2.40
nsepython>=2.95

# Technical analysis
ta>=0.11.0

# News scraping
feedparser>=6.0.10
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=4.9.0

# Sentiment analysis
# vaderSentiment is kept as a fallback if FinBERT load fails.
vaderSentiment>=3.3.2
# FinBERT (finance-tuned BERT) is the default in nlp/sentiment.py — much more
# accurate on financial news than generic VADER. First call downloads ~400MB.
transformers>=4.36.0
torch>=2.0.0

# Dashboard
streamlit>=1.37.0   # need st.fragment (stable in 1.37) for non-blocking partial refresh
plotly>=5.18.0
# streamlit-autorefresh intentionally NOT used — it is a Streamlit custom
# component, and any custom component forces Streamlit to import pyarrow,
# which is blocked on Windows by Smart App Control (DLL signing). We use
# `st.fragment(run_every=...)` instead — server-side, no full page rerun.

# Scheduling
APScheduler>=3.10.4

# Database — Postgres driver (Supabase). SQLite is in stdlib.
psycopg[binary]>=3.2.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
tabulate>=0.9.0
