# Archive

Shipped changes, moved here from `openspec/changes/` once their deltas were merged into
`openspec/specs/`.

Kept for provenance: each directory records why a behavior exists and what was in scope at
the time. The *current* behavior always lives in `openspec/specs/` — if the two disagree,
`specs/` wins and the drift is a bug.
