# Changes

Proposed modifications live here, one folder per change, until archived. See
`openspec/AGENTS.md` for the workflow.

```
changes/<short-id>/
  proposal.md   — why, and what's in/out of scope
  tasks.md      — implementation checklist
  design.md     — technical approach (only when the approach needs explaining)
  specs/<capability>/spec.md   — delta: ## ADDED / MODIFIED / REMOVED Requirements
```

This folder is empty as of the OpenSpec scaffolding change — the next entry here is
expected to be the fundamental-analysis enhancement discussed alongside adopting
OpenSpec, targeting `specs/fundamentals-quality-scoring/`.
